# ASTAL
# Run "ags quit" then "ags run" to apply new colors.
# If you want to change the colors, do so in ~/.config/ags/config/styles.json

# APPEARANCE
general {
  col.active_border = rgb(30,30,30)
  col.inactive_border = rgb(20,20,20)
  
  gaps_in = 2
  gaps_out = 4
  gaps_workspaces = 50
  border_size = 2
}
  
decoration {
  rounding = 8
  
  shadow {
    color = rgba(30,30,30,1)
    color_inactive = rgba(20,20,20,1)
    }
  }
  
  plugin {
    overview {
      panelColor = rgba(20,20,20,0.85)
      panelBorderColor = rgb(20,20,20)
      workspaceActiveBorder = rgb(30,30,30)
      workspaceInactiveBorder = rgb(20,20,20)
    }
    hyprbars {
        bar_height = 24
        bar_padding = 10
        bar_button_padding = 5
        bar_precedence_over_border = false
        bar_part_of_window = true

        bar_color = rgb(20,20,20)

        windowrulev2 = plugin:hyprbars:bar_color rgb(1e1e1e), focus:1
    }
  }

# APPS
$terminal = kitty
$webBrowser = floorp
$webBrowserAlt = firefox
$fileManager = nautilus
$fileManagerAlt = ranger
$textEditor = gnome-text-editor --new-window
$codeEditor = code
$settings = XDG_CURRENT_DESKTOP='gnome' gnome-control-center
$systemMonitor = gnome-system-monitor
$volumeMixer = pavucontrol -t 1
$terminalAlt = foot
    